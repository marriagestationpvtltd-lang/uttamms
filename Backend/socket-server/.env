# =============================================================
# Marriage Station Socket.IO Server — Environment Configuration
# =============================================================

# ── Server ───────────────────────────────────────────────────
# Port the Socket.IO server listens on
PORT=3001

# ── Database ─────────────────────────────────────────────────
# Same MySQL database as the PHP backend
DB_HOST=localhost
DB_PORT=3306
DB_NAME=ms
DB_USER=root
DB_PASSWORD=
DB_CONNECTION_LIMIT=300
DB_QUEUE_LIMIT=20000

# ── CORS ─────────────────────────────────────────────────────
# Comma-separated list of allowed origins for Socket.IO connections.
# Includes web admin panel, mobile apps, and localhost for development
ALLOWED_ORIGINS=*

# ── File Uploads ─────────────────────────────────────────────
# Directory where chat images and voice messages are stored
UPLOAD_DIR=./uploads

# Public base URL used to build file URLs returned by the /upload endpoint.
# REQUIRED for production to guarantee correct HTTPS image URLs.
# Set this to your server's public HTTPS base URL (no trailing slash).
PUBLIC_URL=http://192.168.18.208:3001

# ── Redis (required for multi-instance / high-concurrency mode) ──────────────
# Redis is used by the @socket.io/redis-adapter to synchronise events and
# rooms across multiple Node.js worker processes or server instances.
# Set REDIS_ENABLED=false to disable the adapter (single-instance / dev mode).
REDIS_ENABLED=false
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
# Leave blank if Redis has no password
REDIS_PASSWORD=
# Optional direct URL override, e.g. redis://:password@127.0.0.1:6379
REDIS_URL=
# If true, server startup fails when Redis adapter cannot connect.
REDIS_REQUIRED=false

# ── Call Signaling ────────────────────────────────────────────
# Set CALLS_ENABLED=false to disable all call-related socket events
# (call_invite, call_accept, call_reject, etc.) while keeping chat working.
CALLS_ENABLED=true

# ── Authentication Mode ──────────────────────────────────────
# Set STRICT_AUTH=false in local development to bypass token validation failures
# and allow both admin/user sockets to connect with just userId.
# IMPORTANT: Keep this true in production.
STRICT_AUTH=false

# ── API Configuration ────────────────────────────────────────
# Base URL of the PHP API, used to resolve relative profile-picture paths.
# Defaults to the known production domain; override with API_BASE_URL env var.
API_BASE_URL=http://127.0.0.1/uttamms/Backend

# ── High Concurrency Tuning ───────────────────────────────
# Short cache collapse window to reduce duplicate get_chat_rooms queries
CHAT_ROOMS_CACHE_TTL_MS=2500
# Frequent sweep to keep cache and in-flight maps bounded
CHAT_ROOMS_CACHE_SWEEP_MS=15000
